# Generated by Cargo
# will have compiled files and executables
debug
target

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# Generated by cargo mutants
# Contains mutation testing data
**/mutants.out*/

# RustRover
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/

# Custom removals below

# Builds the driver, will not be providing a way to build the driver due to security concerns.
build_driver.bat 

# Lists all the commands needed to build, sign and deploy the driver. Also not providing for security reasons.
commands.md

# Driver file, will provide in final release.
CpuTempDrv.sys

# OBJ file from built driver, not needed in final release.
cpu_temp_drv.obj

# Executables and object files from the read_cpu_temp utility, not needed in final release.
read_cpu_temp.exe 
read_cpu_temp.obj
READCPUTemp.exe

# Certificate file for signing the driver, not needed in final release. 
MyTestDriverCert.cer 

# All storage stress test logs will not be be provided because they are redundant (you can generate your own!)
log/

Stressor_Installer.exe

